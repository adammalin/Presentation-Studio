import fs from "node:fs";

const standardUrl = new URL("../shared/presentation-design-standard.json", import.meta.url);
const parsed = JSON.parse(fs.readFileSync(standardUrl, "utf8"));

if (parsed?.schema !== "presentation-studio/design-standard" || typeof parsed.version !== "string" || parsed.defaults?.slide?.aspectRatio !== "16:9" || parsed.defaults?.typography?.family !== "Aptos") {
  throw new Error("The packaged Presentation Studio design standard is invalid.");
}

export const DESIGN_CONTRACT_VERSION = parsed.version;
export const DESIGN_CONTRACT = Object.freeze(parsed);

export function designContractMessage() {
  return `Presentation Studio design standard ${DESIGN_CONTRACT_VERSION}: 16:9 and Aptos by default. Resources are immutable inputs; the persisted semantic HTML/CSS Studio Web Scene is the one current presentation. HARD RULE: an existing populated ORNL title slide is sacred and must remain source-preserved exactly; never recompose, restyle, move, resize, add to, remove from, or replace its approved marks, artwork, photography, legal copy, geometry, master, or layout. For a new deck, use an approved ORNL title layout and edit only intended placeholders. For all other slides, reuse matching ready-rated deck design memory, then choose from ranked converted ORNL Template Pack layouts or shared Studio recipes and make a substantive whole-slide composition decision instead of merely shrinking text. When art direction would materially help, create a source-hash-bound visual need: the default prompt exposes abstract structure only, performs an affirmative ORNL Brand Expression Pass, requests one text-free/logo-free raster, and never calls a provider automatically. A generated or supplied concept image is concept-only art direction: follow only its explicitly approved composition, hierarchy, negative-space, color-balance, figure, image-treatment, or rhythm characteristics; never trust or copy generated text, logos, data, claims, or technical details. Analyze it into normalized semantic zones, then call reconstruct_studio_concept to map those approved zones to exact editable source-bound content rather than tracing or flattening the raster. Studio records design impact and refuses to advance or resolve a visual need when the result is unchanged, typography-only, or merely a small cleanup instead of the required material layout, image, figure, or verified diagram reconstruction. Treat technical figures as relationship-preserving groups with explicit crop, focal point, aspect lock, captions, labels, callouts, connectors, information inventory, and verification status. Use high-level optical alignment, equal-gap distribution, snapping, and safe-region constraints instead of guessing correction coordinates. Build that exact scene into editable PowerPoint, compare its native pixels with the original, and run the bounded Found issues, Fixing, Rechecking loop for at most three passes. Converted template designs use a PowerPoint-native artwork base so inherited ORNL imagery and strokes remain visually exact. Slides, comments, critique, build, and export must reference the same scene revision. Preserve approved content exactly and verify the independently rerendered and remeasured export before completion.`;
}
