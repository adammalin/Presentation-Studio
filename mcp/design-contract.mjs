import fs from "node:fs";

const standardUrl = new URL("../shared/presentation-design-standard.json", import.meta.url);
const parsed = JSON.parse(fs.readFileSync(standardUrl, "utf8"));

if (parsed?.schema !== "presentation-studio/design-standard" || typeof parsed.version !== "string" || parsed.defaults?.slide?.aspectRatio !== "16:9" || parsed.defaults?.typography?.family !== "Aptos") {
  throw new Error("The packaged Presentation Studio design standard is invalid.");
}

export const DESIGN_CONTRACT_VERSION = parsed.version;
export const DESIGN_CONTRACT = Object.freeze(parsed);

export function designContractMessage() {
  return `Presentation Studio design standard ${DESIGN_CONTRACT_VERSION}: ORNL is the default brand target unless the user explicitly specifies otherwise; use the current authorized 16:9 ORNL Template Pack and Aptos. Resources are immutable inputs; the persisted semantic HTML/CSS Studio Web Scene is the one current presentation. Every mutating MCP operation returns only after the latest project revision has been written to the app's local recovery checkpoint; recoverySaved means crash recovery is current, not that a user-named project file or PowerPoint export was created. The single AI access switch automatically shares every embedded Resource at the highest level Studio supports. First read list_decks and obey templateWorkflow. Theme, master, and layout identity outrank organization names when classifying the source, but source classification never silently overrides the ORNL design target. Only an explicit user-selected preserve-source target enters source-template-cleanup; in that mode preserve the detected sponsor/custom master, layouts, artwork, and type system and use brand-neutral source-bound alignment, distribution, safe-region, measured text-fit, geometry, and table-layout proposals with PowerPoint-native Current/Proposal QA. For the default ornl-studio workflow, convert the content into the installed ORNL Template Pack and Studio design system; do not merely shrink the source text or keep weak source coordinates. Exact-content protection covers authored slide objects, table cells, data, names, numbers, units, qualifications, attribution, and meaning-bearing relationships. During an explicit cross-template conversion, inherited source-master branding, logos, mastheads, slide numbers, dates, headers, footers, and decorative furniture are evidence rather than authored content and must be replaced by the approved ORNL template layer; do not report their removal as content loss unless the same wording is also authored on the slide. For a new presentation from Resources, call list_resources, read every required compatible text derivative completely with get_resource_text, inspect get_template_layout_catalog, then call create_studio_presentation once with a complete source-grounded narrative and exact source excerpts. That operation instantiates approved ORNL layouts and shared recipes directly in the native presentation-studio/web-scene JSON; it does not require a starter PowerPoint. HARD RULE: an existing populated ORNL title slide is sacred and must remain source-preserved exactly; never recompose, restyle, move, resize, add to, remove from, or replace its approved marks, artwork, photography, legal copy, geometry, master, or layout. For an imported non-ORNL deck converted to ORNL or a new deck, use an approved ORNL title layout and edit only intended placeholders; after initial compilation, preserve it as the protected native title slide. For all other ORNL-target slides, reuse matching ready-rated deck design memory, then choose from ranked converted ORNL Template Pack layouts or shared Studio recipes and make a substantive whole-slide composition decision instead of merely shrinking text. When art direction would materially help, create a source-hash-bound visual need: expose abstract structure by default, perform the ORNL Brand Expression Pass, request one text-free/logo-free raster, and never call a provider automatically. A generated or supplied concept is concept-only art direction; never trust generated text, logos, data, claims, or technical details. Reconstruct approved characteristics from exact editable source-bound content. Treat technical figures as relationship-preserving groups and use high-level constraints instead of guessed coordinates. Build the exact central scene into editable PowerPoint and run the bounded Found issues, Fixing, Rechecking loop for at most three passes. Then call run_deck_qualification once for the changed candidate. Inspect every candidate contact-sheet page, then inspect the clean full-size source and candidate PowerPoint-native pixels for every slide. Use issue crops or diagnostic overlays only to diagnose findings; they are never the finished design. Record each slide review with record_deck_qualification_review, binding it to the qualification ID, candidate package SHA-256, and both raster hashes. Route slide-design issues through MCP, reproducible engine defects through code plus regression tests, visual-asset needs through governed concept prompts, and ambiguity to a person. If a repair is needed, update the one central Studio scene, rebuild, and create a new immutable qualification attempt; do not rerun unchanged bytes. Compare the objective trend with the prior attempt and hold unresolved automatic work after attempt three. An automated pass means ready for visual review, never better by definition; completion requires every slide reviewed and no blocker or major issue. Converted template designs use a PowerPoint-native artwork base so inherited ORNL imagery and strokes remain visually exact. Slides, comments, critique, qualification, build, and export must reference the same scene revision. Preserve approved content exactly and independently rerender and remeasure the export candidate before completion.`;
}
